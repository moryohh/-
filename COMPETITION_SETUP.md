# تفعيل قائمة الصدارة والتقييم الدوري

تمت إضافة ميزة قائمة الصدارة والتقييم الدوري إلى منصة «نحن معك». الكود البرمجي موجود داخل المشروع، أما قاعدة البيانات فتحتاج إلى تشغيل ملف SQL مرة واحدة في Supabase A.

## أين يوجد الكود؟

الملف المطلوب هو:

`supabase_competition_setup.sql`

وهو موجود في المجلد الرئيسي للمشروع، ولا يحتوي على مفاتيح سرية.

## الخطوة اليدوية الوحيدة

افتح مشروع **Supabase A الخاص بالمنصة التعليمية**، وليس Supabase B الخاص بالمجتمع. من القائمة الجانبية اختر **SQL Editor**، ثم اضغط **New query**. افتح ملف `supabase_competition_setup.sql` من مستودع GitHub، انسخ محتواه كاملًا، الصقه في نافذة SQL Editor، ثم اضغط **Run**.

إذا ظهرت رسالة نجاح، أعد فتح المنصة. ستبدأ المنصة عندها بتسجيل نقطة نشاط واحدة لكل خمس دقائق من الاستخدام الفعلي، بحد أقصى خمس نقاط نشاط في اليوم. تُضاف النقطة إلى مجموع نقاط المستوى عن طريق دالة آمنة في قاعدة البيانات.

## قواعد التقييم

| الحالة | النتيجة |
|---|---|
| المستوى من 1 إلى 5 | لا تظهر الشارة الدورية بعد |
| بعد تجاوز المستوى 5 مع دقة أعلى من 70% | ماسي |
| بعد تجاوز المستوى 5 مع دقة 50% إلى 70% | ذهبي |
| بعد تجاوز المستوى 5 مع دقة 25% إلى أقل من 50% | فضي |
| بعد تجاوز المستوى 5 مع دقة أقل من 25% أو دون إجابات | برونزي |

يُحسب التقييم من نتائج MCQ، وصح وخطأ، وجيبها صح، والاختبار اليومي التي تسجلها المنصة، ويُعاد بدء فترة التقييم كل أسبوعين. أما قائمة الصدارة فترتب المستخدمين حسب مجموع نقاط المستوى، ثم المستوى، وتعرض اسم المستخدم وصورته إن كانت متوفرة.

## إذا لم تشغّل ملف SQL بعد

ستظل المنصة تعمل بصورة طبيعية، لكن قائمة الصدارة ستعرض رسالة تفيد بأن النتائج غير متوفرة بعد، ولن تُسجل نقاط النشاط أو نتائج التقييم في القاعدة. بعد تشغيل الملف وإعادة فتح الموقع يبدأ النظام بالعمل.

لا تضع أي `service_role key` أو كلمة مرور داخل ملف SQL أو داخل GitHub؛ الملف مصمم للعمل مع جلسة المستخدم والمفتاح العام الموجود أصلًا في المنصة.
